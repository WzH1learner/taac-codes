# 实验记录索引

后续所有实验结论统一写入本目录。单次实验日志不直接提交到活跃文档区，先用精炼结论记录。

## 记录模板

```text
实验名称：
日期：
目标问题：
改动变量：
训练配置：
best valid AUC：
LogLoss / Brier：
prob_mean / label_mean：
official eval AUC：
推理耗时：
结论：
是否进入蓝图：
```

## 当前主线

| 优先级 | 方向 | 状态 |
|---|---|---|
| P0 | `sparse_lr=0.08` official eval | 待回填 |
| P0 | dense lr warmup | 待实现 |
| P1 | sequence recency / interval stats | 待设计 |
| P2 | item-pair 特征 | 待设计 |
| N1 | skipped high-cardinality hash embedding | 待深入 |
| N2 | target-conditioned sequence pooling | 待深入 |

## 归档说明

历史长日志和阶段性研究笔记已进入 `docs/archive/` 或根目录 `archive/`。它们只用于追溯，不再作为当前决策入口。
