# 当前实验交接

更新时间：2026-05-13

本页给今晚和明天早上的执行顺序。完整 ledger 见
[`../EXPERIMENT_TRACKING.md`](../EXPERIMENT_TRACKING.md)，长期路线见
[`../1-核心蓝图/推荐算法优化蓝图.md`](../1-核心蓝图/推荐算法优化蓝图.md)。

## 1. Current Best

```text
official eval AUC = 0.818095
experiment = D01_grouped_user_dense_single_token
```

当前主线配置：

```text
seq_encoder_type = transformer
ns_tokenizer_type = rankmixer
user_ns_tokens = 5
item_ns_tokens = 2
num_queries = 2
d_model = 64
rank_mixer_mode = full
emb_skip_threshold = 5000000
seq_max_lens = seq_a:128,seq_b:128,seq_c:256,seq_d:256
loss_type = bce
sparse_lr = 0.05
dropout_rate = 0.01
user_dense_projector_type = grouped
use_time_context = 0
use_seq_recent_stats = 0
reinit_sparse_after_epoch = 0
reinit_cardinality_threshold = 0
checkpoint_select_metric = auc
```

旧 `0.809921` 只保留为 fallback 记录，不再是 current best。

## 2. 已确认实验

| 实验 | 单变量 | Best Epoch | Best Valid AUC | Valid LogLoss | Official Eval AUC | 结论 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| E00_reproduce_080992 | `transformer + flat user_dense + no time_context` | 5 | `0.860380` | `0.226409` | `0.808496` | 作为 transformer flat control；不是旧 `0.809921` 的严格复现。 |
| T01_time_context | E00 + `use_time_context=1` | 4 | `0.860832` | `0.225411` | `0.801715` | 拒绝。绝对/root 时间接法存在 official 分布偏移风险。 |
| D01_grouped_user_dense_single_token | E00 + `user_dense_projector_type=grouped` | 6 | `0.864399` | `0.224305` | `0.818095` | 当前新 best。 |

## 3. 当前正在跑

```text
D02_grouped_dense_swiglu
```

只改：

```text
seq_encoder_type: transformer -> swiglu
```

判定：

```text
D02 > 0.818095:
  切到 swiglu + grouped dense。
0.816 <= D02 <= 0.818095:
  D01 仍是主线，D02 作为近似候选。
D02 < 0.816:
  保持 transformer + grouped dense。
```

## 4. 今晚第二个训练：R01

```text
R01_seq_recent_stats_residual_v1
```

目标：验证相对 recency / recent-window intensity，而不是复用失败的 T01。

R01 只加：

```text
use_seq_recent_stats = 1
seq_recent_stats_gate_init = 0.1
```

R01 不做：

```text
不加 weekday/hour/root timestamp bucket
不把时间特征加到 user_dense token
不新增 RankMixer token
不改 sparse_lr/loss/dropout/seq_len/emb_skip_threshold
不开 time_context
不开 group tokenizer
不开 hash
不开 DIN
```

R01 每个样本产出 `seq_recent_stats_dim=28`，即 4 个 sequence domain * 7 维：

```text
len_log
last_gap_log
mean_gap_log
recent_count_30m
recent_count_2h
recent_count_1d
recent_ratio_2h
```

模型接法：

```text
final_repr = final_repr + seq_recent_stats_gate * MLP(seq_recent_stats)
```

`use_seq_recent_stats=0` 时不创建 R01 参数，不改变 D01 state dict。

R01 命令块：

```bash
python3 -u "${SCRIPT_DIR}/train.py" \
    --seq_encoder_type transformer \
    --ns_tokenizer_type rankmixer \
    --user_ns_tokens 5 \
    --item_ns_tokens 2 \
    --num_queries 2 \
    --ns_groups_json "" \
    --user_dense_projector_type grouped \
    --use_time_context 0 \
    --use_seq_recent_stats 1 \
    --seq_recent_stats_gate_init 0.1 \
    --emb_skip_threshold 5000000 \
    --batch_size 128 \
    --num_workers 8 \
    --seq_max_lens "seq_a:128,seq_b:128,seq_c:256,seq_d:256" \
    --num_epochs 20 \
    --sparse_lr 0.05 \
    --dropout_rate 0.01 \
    --patience 15 \
    --reinit_sparse_after_epoch 0 \
    --reinit_cardinality_threshold 0 \
    --loss_type bce \
    --amp \
    --amp_dtype bfloat16 \
    "$@"
```

明天是否 eval：

```text
R01 best valid AUC >= 0.864399:
  优先考虑 official eval，除非 D02 valid 更强。
R01 valid AUC 接近 0.864399 且 LogLoss/Brier/prob_mean 更健康:
  列为候选。
R01 valid 明显低或校准崩:
  不 eval。
如果 D02 valid 更好:
  优先 D02。
```

训练后必须记录：

```text
best epoch
best valid AUC
valid LogLoss
valid Brier
valid prob_mean / label_mean
checkpoint path
train_config.json: use_seq_recent_stats, seq_recent_stats_dim, gate init
checkpoint 是否包含 model.pt / schema.json / train_config.json
与 D01 best valid=0.864399 的差值
```

## 5. P2 只做 EDA

新增脚本：

```text
research/code/pair_match_eda.py
```

用法：

```bash
python research/code/pair_match_eda.py \
    --data_path "${TRAIN_DATA_PATH:-/data_ams/academic_training_data}" \
    --max_files 10 \
    --max_rows 50000 \
    --output research/reports/pair_match_eda.md
```

目的：看 `item_int fid x seq sideinfo fid` 是否存在 target-history exact match lift。

只看报告，不训练。只有报告显示 match rate 非极端且 lift 稳定，再考虑
`P2_pair_dense_v1`。

## 6. S01 Seed Sweep，延后

候选：

```text
S01_D01_seed_sweep
seeds = 42, 2026, 3407, 1234, 2025
```

原则：

```text
基线使用 D01 或 D02/R01 中明天表现最好的主线
只改 --seed
不改模型结构和训练参数
先看 valid AUC、LogLoss、prob_mean/label_mean
只有 valid 明显超过当前主线才考虑 official eval
```

注意：CUDA / SDPA / IterableDataset 可能导致非完全确定性，seed sweep 只能作为微调收益，不能替代结构性涨点。不要把 seed sweep 和 R01/hash/G01 混在同一次实验。

## 7. 禁试清单

本轮不要做：

```text
D01 + T01
D01 + sparse_lr=0.08
D01 + dense warmup v1
D01 + DIN v1
D01 + old UE split v1
D01 + focal/loss/dropout/参数量混合包
D01 + time_window
D01 + use_seq_time_delta_proj
D01 + seq*2
D01 + 7M threshold
hash embedding 训练
G01 group tokenizer 训练
```
